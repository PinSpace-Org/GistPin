# Base URL of the GistPin backend API (no trailing slash, no /v1 suffix).
# Run the backend locally with `cd Backend && npm run start:dev` (port 3000).
NEXT_PUBLIC_GISTPIN_API_URL=http://localhost:3000
