export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-3xl flex-col justify-center gap-4 px-6">
      <h1 className="text-3xl font-semibold">GistPin Analytics</h1>
      <p className="text-neutral-500">
        The app scaffold is in place. Dashboards, the API client and shared UI
        are built up through the open analytics issues.
      </p>
    </main>
  );
}
