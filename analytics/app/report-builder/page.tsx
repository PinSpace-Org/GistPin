import ReportBuilder from '@/components/ReportBuilder';

export default function ReportBuilderPage() {
  return (
    <ReportBuilder
      heading="Build a custom report"
      description="Drag or add widgets into the canvas, resize and reorder them, then save the layout or share it by URL."
    />
  );
}
