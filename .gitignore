node_modules/

# Build output
dist/
.next/
coverage/

# Rust / Soroban
target/
test_snapshots/
*.wasm

# Cilium CLI binary - fetched on demand, never committed (see scripts/fetch-cilium.sh)
cilium-linux-amd64.tar.gz
cilium-linux-amd64.tar.gz.sha256sum
cilium

# Environment
.env
.env.local

# OS / editor
.DS_Store

# Dependency health check reports (generated locally/in CI, not committed)
infrastructure/ci/reports/
