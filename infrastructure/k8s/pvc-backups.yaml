apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backups-pvc
  namespace: gistpin-prod
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: gistpin-ssd
  resources:
    requests:
      storage: 200Gi
