apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-pvc
  namespace: gistpin-prod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gistpin-ssd
  resources:
    requests:
      storage: 100Gi
