apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: gistpin
spec:
  selector:
    app: gistpin-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
